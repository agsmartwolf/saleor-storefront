query ProductListByCollection($slug: String!, $locale: LanguageCodeEnum = EN) {
	collection(slug: $slug, channel: "georgia") {
		name
		description
		seoDescription
		seoTitle
		products(first: 100) {
			edges {
				node {
					...ProductListItem
				}
			}
		}
	}
}
