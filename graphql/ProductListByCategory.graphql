query ProductListByCategory($slug: String!, $locale: LanguageCodeEnum = EN) {
	category(slug: $slug) {
		name
		description
		seoDescription
		seoTitle
		products(first: 100, channel: "georgia") {
			edges {
				node {
					...ProductListItem
				}
			}
		}
	}
}
