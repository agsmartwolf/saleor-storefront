query ProductListPaginated($first: Int!, $after: String, $locale: LanguageCodeEnum = EN) {
	products(first: $first, channel: "georgia", after: $after) {
		totalCount
		edges {
			node {
				...ProductListItem
			}
			cursor
		}
		pageInfo {
			endCursor
			hasNextPage
		}
	}
}
